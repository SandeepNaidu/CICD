language: scala
scala:
   - 2.12.7

services:
- docker

before_install:
- docker build -t SandeepNaidu/CICD .
- docker run -d -p 127.0.0.1:80:4567 SandeepNaidu/CICD /bin/sh -c "cd /root/CICD; bundle exec foreman start;"
- docker ps -a
- docker run SandeepNaidu/CICD /bin/sh -c "cd /root/CICD; sbt test" #"bundle exec rake test"

script:
#- bundle exec rake test
- sbt test